# Copy to .env and fill in values. Never commit .env to Git.

# Server
PORT=3000
NODE_ENV=development

# Database (MySQL) - cloud: PlanetScale, Railway, Aiven, etc. See DEPLOY_RENDER_GITHUB.md
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=doctor_appointment

# Gemini API (for chatbot) - get key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# Optional: frontend URL for CORS (e.g. https://your-app.onrender.com)
FRONTEND_URL=http://localhost:4000
